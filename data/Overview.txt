Subfolders:
raw/:

Stores original brightfield microscopy images.
Used as the source of truth for reproducibility.
Should be immutable—never overwritten.
processed/:

Contains preprocessed images (e.g., CLAHE-enhanced, segmented).
Used for training and inference.
Generated by scripts in src/preprocessing/.
metadata/:

CSV/JSON files with labels (e.g., differentiation stage), timestamps, and possibly gene expression profiles.
Used for supervised learning and multi-modal integration.